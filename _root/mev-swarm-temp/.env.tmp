# REMOVED: plaintext secret removed on security remediation.
# Never store secrets in the repository. Use a secrets manager or CI secret store.
# Example placeholder (do NOT use in production):
# PRIVATE_KEY=0xREDACTED_PLACE_IN_SECRETS_MANAGER

# If you need a local test key, create a local `.env.local` that is gitignored
# and add the real secret there, or set the environment variable at runtime.