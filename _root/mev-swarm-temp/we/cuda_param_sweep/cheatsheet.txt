Run everything (audio feedback)
./run_all.sh

Run only the low-range sweep (GPU 0)
./run_sweep_part1.sh

Run only the high-range sweep (GPU 1 or same GPU)
./run_sweep_part2.sh

Generate PTX/SASS for a specific size
TOTAL_PARAMS=2048 ./generate_ptx_sass.sh

Profile the last run (10s trace)
nsys profile --duration=10 -o last_trace ./run_all.sh
nsys stats last_trace.nsys-rep > last_summary.txt
